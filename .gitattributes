# i18n 生成物由 scripts/generate-i18n-resources.mjs 按 LF 写出。
# 本机 core.autocrlf=true（仓库无 .gitattributes）会把 checkout 落成 CRLF，
# 于是「生成器跑前 / 跑后哈希」必然不等 ——
# 实测：**在全新 worktree / 全新 checkout 里第一次跑必假红**，重跑一次才 PASS。
# 把这 6 个路径固定成 LF，让工作区与生成器输出一致（本机实测：加之前首次必红，加之后稳定绿）。
entry/src/main/resources/base/element/string.json text eol=lf
entry/src/main/resources/zh_CN/element/string.json text eol=lf
entry/src/main/resources/en_US/element/string.json text eol=lf
entry/src/main/ets/core/i18n/I18nKeys.ets text eol=lf
.scratch/foundation/i18n-keys.json text eol=lf
.scratch/foundation/i18n-key-map.md text eol=lf

# 字典正本（scripts/i18n-reference/*.ts）是生成器的**输入**，也必须钉成 LF：
# 字典里有跨行模板字符串，CRLF 会被逐字读进取值。实测（2026-09-15）：把这两份文件
# CRLF 化后生成器仍然成功，但三个 string.json 的 SHA256 全变
# （base 9776D7DD…→BDBBAE1F…、zh_CN 790C2DA9…→DC2D54B8…、en_US 13A726B7…→A7F82DD6…）——
# 即 core.autocrlf=true 的机器克隆后生成物会与版本库不一致。钉 LF 后 checkout 落回 LF。
scripts/i18n-reference/zh.ts text eol=lf
scripts/i18n-reference/en.ts text eol=lf

# Mock 样例文件是**二进制**，而且这一点是硬要求（不是洁癖）：
#   - PDF 的 xref 偏移与内容流的 /Length 都按**字节**算，checkout 落成 CRLF 会让
#     PDFKit / 任何阅读器再也打不开（PDF 里没有 NUL 字节，git 会把它当文本！）；
#   - ZIP 的本地头与 CRC 更经不起整体改写。
# 本机 core.autocrlf=true，实测（git checkout-index）：不加这一条时三份 PDF 在
# 全新 checkout 里分别变成 3977 / 2739 / 4610 字节（= 每个 LF 多一个 CR），
# scripts/check-mock-files.mjs 会 FAIL。pptx 恰好被判成二进制而幸免，不能靠运气。
# 用 binary（= -text -diff -merge -partial）而不是 text eol=lf：这些是二进制资产。
entry/src/main/resources/rawfile/mock-files/** binary
