# 16 页面骨架统一：返回图标 + 与标题同行、内容靠上（过程证据）

设备：`127.0.0.1:5557`，实测 `const.product.devicetype=phone`、`const.ohos.apiversion=23`
（模拟器，非真机）；窗口 1320×2856px、密度 3（= 440×952vp）。
判据全部取 **layout dump 的 px**；帧只做同状态对照。**帧 / dump / hilog 只留本地** `.dsh/logs/`，不入库。

取证对应的提交：`110dab8`（= `71fd38d` 我的改动 + `main` 的 t14 合并）；取证时 `git status --porcelain` 为空。
第一次「修后」取证（`aft-*`）用的是**合并前**的 `71fd38d` 产物；合并后重编重装，第二次「修后」取证（`fin-*`）
用的是 `110dab8` 的产物，两条都是同页同状态的设备证据。

## 1. 派单情报的两处修正（先核实、再动手）

| 统筹者初查 | 实测 | 证据 |
| --- | --- | --- |
| 「纵向居中候选：`FilesPage.ets` 的 `justifyContent(FlexAlign.Center)`（约 :542/:552）」 | 方向对、位置错：那两处只覆盖**加载态 / 失败态**；文件列表本身一直是靠上的 | 修前 dump `pre-12-files.json`：表头 y0=234、筛选片 y0=360；而「暂无文件」空态 y0=1388。失败态另一次（`r-07-files.json`）失败文案 y0=1457（视口 y∈[437,2562] 的中部）。源码 :533-556 |
| 「设置页里 grep 没命中不代表没有」 | 命中在**四个子页的 `Scroll`**，不在设置 tab 根 | 修前 `r-04-filesettings.json`：`Scroll` 视口 y∈[361,2562]，子 `Column` 实测 y∈[972,1952] ⇒ 顶部空 611px；`r-02-immersive` / `r-05-help` / `r-06-about` 同形 |

## 2. 盘点表（修前，全部 dump px）

- **同行** = 返回节点与标题节点的 **y 区间有重叠**。
- **靠上** = 首个内容节点的 y0 紧随页头，而不是落在容器视口中部。

| 页面 | 返回形态 | 返回 bounds | 标题 bounds | 同行？ | 内容 |
| --- | --- | --- | --- | --- | --- |
| 设置 tab（tab 根，**无返回**） | — | — | `设置` y[192,290] | — | 靠上：首行 y0=318 |
| 文件 tab（tab 根，**无返回**） | — | — | `文件` y[234,332] | — | 靠上：筛选片 y0=360 |
| 沉浸式模式 | **文字**「返回」 | x[56,155] y[178,235] | `沉浸式模式` y[263,361] | **否**（235 < 263） | **居中**：内容 y0=1023，顶空 662px |
| 学期切换 | **文字**「返回」 | x[56,155] y[192,249] | `学期切换` y[277,375] | **否**（249 < 277） | 靠上：首行 y0=389 |
| 文件设置 | **文字**「返回」 | x[56,155] y[178,235] | `文件设置` y[263,361] | **否** | **居中**：内容 y0=972，顶空 611px |
| 帮助与反馈 | **文字**「返回」 | x[56,155] y[178,235] | `帮助与反馈` y[263,361] | **否** | **居中**：内容 y0=674，顶空 313px |
| 关于 | **文字**「返回」 | x[56,155] y[178,235] | `关于` y[263,361] | **否** | **居中**：内容 y0=677，顶空 316px |
| 课程详情 | **图标**（arrow-back） | 单独 `Stack`（源码 :159-171） | 课程名在**下一行**（`.margin({top: space2})`） | **否** | 靠上 |
| 公告详情 | **图标**（arrow-back） | `headerRow` 内 | 同行文字是分区名（`ui_tab_notices`） | **是** | 靠上 |
| 作业详情 | **图标**（arrow-back） | `headerRow` 内 | 同行文字是分区名（`loh_assignments`） | **是** | 靠上 |
| 文件详情 | **图标**（arrow-back） | `topBar` 内 | 该页**无标题**（工具栏 + 内容） | —（无标题可并） | 靠上 |

**统一到哪一边**：以**公告详情 / 作业详情 / 文件详情**（本工程里已经是图标、返回与它的同行文字同处一个 `Row`）
为样板：返回 = 图标闭集里的 `AppIcon.ARROW_BACK`（不新造字形），与标题**同一 `Row`**、`VerticalAlign.Center`；
标题**不再另占一行**；内容容器一律**靠上**。

**站点事实（不归本次改动）**：5557 上课程 tab / 文件 tab 均为 0 条
（`semester resolution failed: no xnxq in current semester response: bytes=69`，
见 `pre-10-notices.json` / `pre-12-files.json`）⇒ 课程详情页**没有可点进去的入口**。

## 3. 改动清单（源码，提交 `71fd38d`）

| 文件 | 改动 |
| --- | --- |
| `features/files/FileSettingsPage.ets` | 返回文字 → `IconGlyph(ARROW_BACK)`；与 `文件设置` 同 `Row`；`Scroll` 加 `.align(Alignment.Top)` |
| `features/courses/SemesterSelectionPage.ets` | 返回文字 → 图标并与 `学期切换` 同行；页头 padding 收成 `space3/space2`（与其余子页一致） |
| `features/settings/ImmersiveSettingsPage.ets`、`HelpPage.ets`、`AboutPage.ets` | 同上（图标 + 同行 + `Scroll` 靠上） |
| `features/courses/CourseDetailPage.ets` | 返回从标题**上一行**移进标题所在 `Row`；教师名留在下一行 |
| `features/files/FilesPage.ets` | 加载态 / 失败态 `.align(Alignment.Top)`（失败文案不再落在视口中部） |

未改：公告详情 / 作业详情 / 文件详情（本来就是样板）；设置 tab 根、文件 tab 根（本来就靠上）。
`loh_back` 仍是图标的无障碍 label 与字体未就绪回退文案 ⇒ **未新增键、未删键**。

## 4. 判据对照（修后 = `fin-*`，提交 `110dab8`）

### 4.1 返回与标题**同行**（y 区间重叠）

| 页面 | 修前 返回 / 标题 y | 修后 返回 / 标题 y | 修后重叠 |
| --- | --- | --- | --- |
| 文件设置 | [178,235] / [263,361] → 不重叠 | **[206,290] / [199,297]** | 是（204→290 与 199→297 重叠） |
| 学期切换 | [192,249] / [277,375] → 不重叠 | **[206,290] / [199,297]** | 是 |
| 沉浸式模式 | [178,235] / [263,361] → 不重叠 | **[206,290] / [199,297]** | 是 |
| 帮助与反馈 | [178,235] / [263,361] → 不重叠 | **[206,290] / [199,297]** | 是 |
| 关于 | [178,235] / [263,361] → 不重叠 | **[206,290] / [199,297]** | 是 |

修后的「返回」节点是 `IconGlyph` 的 `Text`（`x[84,168] y[206,290]`，空 text = 图标字形码位，
不是回退文案「返回」）；标题 `x[238,1264] y[199,297]`。两侧中心 y 分别是 248 与 248 ⇒ 同行且垂直居中。
**字号/字重/颜色/宽度未动**（仍是 `headlineSmall` + Bold + `onSurface`），标题左边界由 56 变 238
（让位给 40vp 的返回点击区 + `space3`），标题**上移** 64px。

同一行里返回不再是文字的依据（同一支 dump 的对照）：修前 `r-04-filesettings.json` 里
`Text x[56,155] y[178,235] text='返回'`；修后 `fin-04-filesettings.json` 里同位置是**空 text 节点**
`x[84,168] y[206,290]`，且 `aft-04-filesettings.png` / `fin-04-filesettings.png` 帧里是紫色返回箭头。

### 4.2 内容**靠上**

| 页面 | 修前 内容首节点 y0 | 修后 内容首节点 y0 | 变化 |
| --- | --- | --- | --- |
| 文件设置 | 972（`Column` 容器）/ 1030（首个 Text） | **404**（首个 Text） | 上移 **626px** |
| 沉浸式模式 | 1023 / 1135 | **456**（首个 `TableCell` 图标）/ 458 | 上移 **567px** |
| 帮助与反馈 | 674 | **486** | 上移 **188px** |
| 关于 | 677 | **486** | 上移 **191px** |

### 4.3 不回归（其余按钮位置不变）

- 文件 tab 页头（搜索入口）：修前 `pre-12-files.json` 与修后 `fin-12-files.json` 的
  `Tabs/Swiper/TabContent/Row/Column` 前缀完全一致，搜索图标 `Text x[1152,1236] y[220,304]` 逐字节未变
  （该页本次未改，作为「dump 口径本身稳定」的对照）。
- 设置 tab 根：页头 `设置` 仍是 `y[192,290]`、首行 y0=318（未改，未动）。
- 各子页**除了**页头与内容容器外没有其它按钮；文件详情 / 公告详情 / 作业详情的顶栏未改。

## 5. 门禁（合并后 `110dab8`）

- 单测：`entry/.test/.../test_result.txt`（mtime 2026-09-13 16:25:09）
  `Tests run: 427, Failure: 0, Error: 0, Pass: 427, Ignore: 0`；测试日志 `ERROR` 命中 0。
- 打包：`assembleHap --no-incremental` 日志（`t16-build2.log`）`ERROR`/`ErrorCode` 命中 **0**；
  `Finished :entry:default@CompileArkTS`、`BUILD SUCCESSFUL in 13 s 109 ms`；hap mtime 16:25:35。
- 四脚本：`check-domain-purity` PASS / `check-import-graph` PASS（WARN 两条入口文件，正常）/
  `check-i18n-keys` `RESULT: OK`（182 个引用键全部解析）/ `check-generated-fresh` PASS。

## 6. 未做到 / 存疑

- **课程详情的设备对照取不到**：5557 上课程列表 0 条（站点当前学期解析失败），没有可点进去的入口。
  该页「修前」是源码级判据（原 :157-186 的 `Stack` 与 `Text` 分处两层、`margin({top: space2})`），
  「修后」是源码 + 构建产物 + 同一套 `Row` 在其余五页的 dump 证据，**没有本页的设备 dump 对照**。
- **文件页失败态的「修后」设备帧没抓到**：该页当前走的是空态（`屏蔽 4`，全部 0），
  失败态要等站点失败才出现。失败态「靠上」的依据是源码（`.align(Alignment.Top)`，与设置页
  已验证生效的同一属性）+ 修前 `r-07-files.json` 的失败文案 y0=1457 的对照。**没抓到不等于不存在**。
- **设置 tab 根（无返回按钮）本来就靠上**，本次未改；票面「设置页内容纵向居中」实测落在它的**子页**。
- **分栏态未取证**：设备只有 phone，分栏态不归本 ticket（未触碰 5559）。
