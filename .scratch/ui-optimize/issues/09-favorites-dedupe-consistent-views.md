# 09: 收藏去重与三视图口径统一

**What to build:** 同一条内容项被收藏两次不再产生重复 id；收藏 / 归档 / 屏蔽三个视图的口径统一——
「全部」排除已归档与被隐藏课程的内容项，「收藏」是「全部」的子集，「归档」「屏蔽」沿用同一套排除规则；
在屏蔽视图里，一条已经收藏过的内容项要显示为「已收藏」。

**Blocked by:** None（可立即开始）

**Status:** verified（统筹者复核：代码已合并、断言反转范围正确、设备侧消费点实证闭环）

- [x] 对同一内容项连续两次置真，收藏集合里只有一条记录
- [x] 「屏蔽」视图里，已收藏内容的收藏状态显示为已收藏（不再显示成未收藏）
- [x] 三视图口径统一后，「全部」与「收藏」的计数与列表自洽
- [x] 既有那几条钉住「置真 append 不去重」的断言**反转为去重语义**；这是有意的行为变更，在交付说明里写清
- [x] 其余既有收藏/归档/屏蔽断言仍全绿
- [x] 证据：单测输出 + 设备上一次「收藏 → 屏蔽该课程 → 看屏蔽视图」的截图
## Comments

### 2026-09-13 · 统筹者验收：verified

- **验收清单全部达成**，其中最关键的一条不是截图而是**消费点实证**（一条论断一份证据）：
  在「屏蔽」视图里点收藏按钮 ⇒ hilog `favorite set … flag=false favorites=notice:1` —— 只有**行状态取自原始收藏集合**（读到「已收藏」）才会发出「置假」；旧行为（取过滤后的 fav 分组）会读到未收藏、从而**再写一条重复 id**。
  这条同时否证了「没改」与「没生效」两种可能。配套截图 `.scratch/ui-optimize/evidence/t09/09-…swiped-heart-favorited.png`。
- **我独立复核**：断言反转只动了该动的几条（`n1,n1 → n1`、批量归档 `['n2','n2']` 去重、屏蔽课程去重 + 新增 `aFavoritedItemStaysFavoritedAfterItsCourseIsHidden`）；
  `favoriteIsASubsetOfAll*` / `archivedAndHiddenGroupsKeepTheOtherFilterOut` / `allExcludesArchived*` / 计数断言**一字未改**，全绿；主树合并态 `Tests run: 392, Failure: 0, Error: 0`、四脚本绿、产物级符号检索（`noticesContain` / `mergeIds`）。
- **去重扩到归档与屏蔽**：统筹者**接受**（spec 的实现决定表写的是「置真改为去重」，未限定只有收藏；ticket 验收第 3 条又要求三视图口径统一）。
- **口径冲突已由账号所有者裁定**：2026-09-13 —— **「收藏」是「全部」的子集**（同 ticket 与参考实现）。spec 第 75 行原写「收藏不再是「全部」的子集」属笔误，已就地更正。
- **如实登记的既有行为（本 ticket 未改、留作技术债）**：归档视图里，「归档」按钮态取的是归档分组 —— 一条同时被收藏的条目在归档视图里会把归档按钮渲染成「未归档」，且收藏按钮不渲染 ⇒ 在归档视图里点不动「取消归档」。这是既有行为、本 ticket 未要求。