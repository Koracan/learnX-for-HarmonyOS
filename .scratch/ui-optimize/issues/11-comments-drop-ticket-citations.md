# 11: 注释去掉剩余的文档编号引用

**What to build:** 代码注释里不再出现会过期的文档指针——工单编号、验收条款、spec 章节号这类引用全部去掉，保留注释里的技术解释本身。
台账编号引用已在上一轮清完，本 ticket 处理剩下的一批。

**Blocked by:** None（可立即开始）

**Status:** verified（合并 `62105e8`：范围内归零 0 / 负对照 207；产物对照实验证明只动注释；5 处运行期字符串记为例外）

- [x] 代码注释里不再出现 `ticket NN` / `验收第 N 条` / `spec 第 N 节` 这类文档编号引用
- [x] 注释的技术解释逐字保留（「解析层不解码实体」「append 不去重」这类句子不许被削弱）
- [x] 没有引用后失去全部内容的注释行整行删除；注释块仍然配平
- [x] 判定标准：**测试自述意图**的用法（如「本文件逐条锁定该顺序」）不算文档指针，保留
- [x] 证据：全仓 grep 归零 + 注释块配平检查 + 四个脚本绿 + 一次真实编译（`assembleHap`，判成败要搜日志里的 ERROR）

## Comments

### 2026-09-13 · 统筹者派单前的范围界定（**这张 ticket 最后跑、且单独跑**）

**为什么必须最后**：其他每一张 ticket 都在往注释里写东西；它必须在一棵"别人都不再动代码"的树上做，否则刚清完又被写回来。

**规模（我先量过）**：`git grep -n -E "ticket|工单|验收条款|台账"` 在 `entry/src/main/ets/**` + `entry/src/test/**` 下命中 **约 547 行、约 140 个文件**。这是一次**判断密集**的清扫，不是纯删除 —— 计划好时间，别当成小活。

**清理范围（我给的界定，冲突时以本条为准）**：
- **要清掉**：`ticket NN`（含 `ticket 11.5` 这种小数形式）、`工单…第 N 条` / `验收第 N 条`、`spec 第 N 节`、台账编号，以及**没有编号但同样是文档指针**的说法（"本工单"、"工单要求"、"工单原文"、"工单交付节"）。
- **不要动**：① 指向**参考实现**的引用（`reference/learnOH-old/...:行号`）—— 那是本仓移植惯例，且参考实现是稳定工件；② **ADR 引用**（`ADR-0004` 这类）—— 架构决策是长期注册表，不是会过期的文档指针；③ 测试里**自述意图**的用法（"本文件逐条锁定该顺序"）。
- **技术句逐字保留**：注释里的解释必须留下来，只把指针去掉并让句子仍读得通。例：`工单验收第 3 条要求「失败有明确提示且不丢失已填内容」` → `失败要有明确提示且不丢失已填内容`。**不许**把句子削弱成"按需求实现"。
- **只剩指针的注释行整行删除**；注释块仍要配平。
- **范围限于代码注释**：`entry/src/main/ets/**`、`entry/src/test/**`，以及 `scripts/**` 的注释（改注释不影响生成物）。**`docs/**` 与 `.scratch/**` 不在范围内**（两份台账、验收记录、spec 里的编号本来就要留着）。

**我额外要求的自证**：
- **"只动注释"的机械证明**：交一个剥离脚本（或等价的检查）—— 把改动前后两侧的所有注释剥掉，剩下的代码必须**逐字节相同**。这比"我看了 diff"强得多，也是本次改动唯一可能悄悄改坏东西的地方。
- **grep 归零要连正则一起交**：把你最终用的那条正则贴出来，并给出归零的原始输出；再附一条**负对照**（拿一个已知存在的旧串去搜，必须还能命中），证明"归零"不是正则写错。
- 注释块配平检查、四个 node 脚本、`assembleHap --no-incremental`（**搜 `ERROR` / `ErrorCode` / `COMPILE RESULT`**）、以及单测真绿（先删 `entry/.test` + `--no-incremental`，核对 `test_result.txt` 的 `Times run:` 行与时间戳）。

### 2026-09-13 · 统筹者验收：**verified**（合并 `62105e8`）

**两轮交付**：`699f6e9`（171 文件 / 690 加 690 删）+ `d7c14e3`（35 文件 / 47 加 53 删）。第二轮是我退回的 —— 见下面第 3 条。

**我独立复核过的东西**：
- **改动面与范围**：`git diff --numstat` 确认 **171 + 35 个文件**；**没有任何 `docs/` 或 `.scratch/` 路径**被碰；**没有"只删不加"的文件**；**新增行里没有空注释行**（不是把内容删空留空壳）。
- **归零与负对照**（我自己跑他们给的最终正则，范围 `entry/src/main/ets` + `entry/src/test` + `scripts`）：现在 **0 命中**；在改动前的 `7487e44` 上 **207 命中**；纯删除串 `工单验收第 3 条` 在范围内 0、整树仍有 3（那是刻意没动的 `docs/**`、`.scratch/**`）⇒ 负对照成立。
- **技术句没被削弱**（抽看三处 diff）：`SubmissionState.ets` 的"工单验收第 3 条要求「失败有明确提示且**不丢失已填内容**」"→"失败要有明确提示且**不丢失已填内容**"；`FilteredContent.ets` 的"工单验收第 5 条「隐藏课程的内容不出现」"→"「隐藏课程的内容不出现」"；`AboutPage.ets` 只删掉那行"取舍与判据见交付说明。"，前面的技术说明原样。
- **"只动注释"的机械证明**：行级分类器对**全量**改动（`7487e44` → `d7c14e3`）判定 `code diff = 0`，694 条改动行全部是 comment→comment（688）或 comment→deleted（6），UNSAFE = 0。配平 218 文件 / 1970 块注释 / 1096 行注释，unclosed = 0。
- **终验（我在冻结后的 main 上自己跑的）**：删 `entry/.test` + `test --no-incremental` → `test_result.txt` 时间戳 `2026/9/13 15:21:19`、**`Tests run: 424, Failure: 0, Error: 0, Pass: 424`**（与它在自己树里 stash 前后测得的 424 == 424 一致 ⇒ 一条测试都没被删）；`assembleHap --no-incremental` `BUILD SUCCESSFUL`，日志搜 `ERROR`/`ErrorCode`/`COMPILE RESULT` = **0**；四个脚本 PASS / PASS / OK / PASS。

**退回的那一轮（第 3 条，值得记下来）**：第一轮它把**具体指针**（"工单交付节"）改写成了**另一个同样不存在的指针**（"见交付说明"）—— 实测 `交付说明|交付报告|见交付|交付节` 在改动前 18 处、它改完 **19 处**，即**新引入**了 4 处。这类说法比工单号**更糟**：工单号至少能在 `.scratch/ui-optimize/issues/NN.md` 里查到出处，"交付说明"什么也查不到。已要求**整句删掉指针子句**（不是改名），并顺带清掉同一类（`见统筹提示` / `见边界说明` / `见未验证项` / `见相关说明` / `见追加节` / `见该条` / `偏离说明` / `What to build` / `待账号验证`），把模式并进归零正则。

**产物级自证的一个更正（它自己发现并证明的）**：我原先给的判据"注释不该影响产物"对**删行**类改动**不成立** —— 第一轮没删行，`ets/modules.abc` 与基线同一 SHA256；第二轮删了 6 行"只剩指针"的注释后 SHA 变了。它做了**对照实验**定性：把 6 行以**空注释行**补回（让行号回到基线）→ 编译 → **回到基线的 `CB6C11CB…`、0 字节不同**；而与该基线相比只有 **14 字节**不同（3 个 abc 头部校验字段 + 11 个"基线值 − 1"，正对应其后代码的**调试行号前移 1**，Panda abc 把函数起始行号编码成 varint）。⇒ **注释文字对产物零影响**这条仍然成立，但**删注释行会改行号**。我据此在 `docs/agents/gates.md` 新增了陷阱 5。

**记录在案的例外（5 处，全部保留，不改）**：
- `EnrollmentScript.ets:186/187/877`：这 3 行在**注入脚本的模板字符串内部**（我读了 170–198 行上下文，那是注入页面的 JS 本体），是**运行期数据**而非我们源码的注释；改它们会改变产物字节。
- `CourseData.test.ets:256`、`Tokens.test.ets:221`：两个 `it()` **用例名**里的 `ticket 09` / `ticket 11.5`。工单自己写了"测试自述意图的用法不算文档指针，保留"，且改用例名只是改运行期字符串、收益为零。
- 另：`entry/src/test/fixtures/README.md` 里 `docs/reference-quirks.md 第 4 条` 那句**保留**（它解释的是"这条锁定行为从哪来"，删了只剩空话）；同一个 md 里按同口径清掉的 4 处编号也**不回退**（它就在代码树里、描述夹具）。

