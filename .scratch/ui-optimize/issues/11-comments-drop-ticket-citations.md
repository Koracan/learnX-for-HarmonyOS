# 11: 注释去掉剩余的文档编号引用

**What to build:** 代码注释里不再出现会过期的文档指针——工单编号、验收条款、spec 章节号这类引用全部去掉，保留注释里的技术解释本身。
台账编号引用已在上一轮清完，本 ticket 处理剩下的一批。

**Blocked by:** None（可立即开始）

**Status:** ready-for-agent

- [ ] 代码注释里不再出现 `ticket NN` / `验收第 N 条` / `spec 第 N 节` 这类文档编号引用
- [ ] 注释的技术解释逐字保留（「解析层不解码实体」「append 不去重」这类句子不许被削弱）
- [ ] 没有引用后失去全部内容的注释行整行删除；注释块仍然配平
- [ ] 判定标准：**测试自述意图**的用法（如「本文件逐条锁定该顺序」）不算文档指针，保留
- [ ] 证据：全仓 grep 归零 + 注释块配平检查 + 四个脚本绿 + 一次真实编译（`assembleHap`，判成败要搜日志里的 ERROR）