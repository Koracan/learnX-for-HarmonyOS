# 04: 分栏态下底栏只占主栏

**What to build:** 横屏分栏态下，底部 tab 栏只出现在左栏（主栏）下方，而不是横跨整个屏幕；竖屏单栏态保持横跨整屏。
主栏宽度仍是那个既有常量（393vp），不新增断点、不改分栏判定。

**Blocked by:** None（可立即开始）

**Status:** verified-partial（分栏档 + 三态单测 + 主树门禁达成；单栏帧与全屏帧待统筹者补）

- [x] **先取基线**：分栏态下一次 layout dump，记录底栏命中区与主栏宽度（实测基线：主栏 786px、底栏 5×576px 横跨 2880px，密度 2）
- [ ] 分栏态下，底栏命中区宽度等于主栏宽度；单栏态下仍等于整屏宽度
- [x] 分栏判定与主栏宽度常量一字未改（不在本 ticket 里动断点）
- [x] 分栏几何其余部分不变：主栏 786px、分隔条 2px、右栏取余量
- [x] 若基线里出现「主栏内容提前画断」的异常（与既有真机截图一致），**记录并停在归因**，不在本 ticket 里改几何
- [ ] 证据：分栏态与单栏态各一次 layout dump + 截图
## Comments

### 2026-09-13 · 统筹者验收：verified-partial（代码/单测/主树门禁达成；两帧设备证据待统筹者补）

**已达成**：
- **分栏态**（平板 2880×1920 @密度2，改动后装机 layout dump）：底栏 5 个命中区 `[0,1754,157,1864] / [157,…] / [314,…] / [472,…] / [629,…,786]` ⇒ **合计 x∈[0,786]**，与主栏宽 786px 相等（改动前基线是 5×576 横跨 2880）。
  像素级佐证：底栏顶边框扫描 `y∈[1750,1758]`，`nonWhite maxX=787`（基线 `maxX=2879`）。
- **分栏几何其余部分未动**：主栏 `Refresh [0,266,786,1752]` 与基线逐字节相同；右栏空态 `[1786,939,1883,987]` 与基线相同 ⇒ 分隔条 2px、右栏取余量都没变；断点/分栏判定/393vp 常量 `git diff` 零改动。
- **三态纯函数** `bottomBarLayout(splitActive, masterHidden)`（`features/shell/SplitView.ets:136`，接口在 :112）由 `ShellTabs.ets:243-249` 取值、:303/:305 接 `.width()` 与 `.visibility()`。我已在合并后的主树里 grep 复核这条接线。
  三档 + 一条边界都有单测：单栏 ⇒ 可见+整屏；**单栏 + 残留 masterHidden ⇒ 仍可见+整屏**（防底栏凭空消失）；分栏+主栏可见 ⇒ 可见+393vp；分栏+主栏隐藏（文件详情全屏）⇒ **隐藏**。
- **主树合并态**（`458984e` 之后）：四门禁脚本 PASS / OK。

**未达成（统筹者认领，不写成已验）**：
1. **单栏态设备帧**：底栏应仍整宽。模拟器不能旋转/缩放、平板恒横屏 ⇒ 只能在 phone 上补；Pura 90 当时被 ticket 12 占着。
2. **全屏态设备帧**：分栏 → 文件详情 → 全屏 ⇒ 底栏消失；退出全屏 ⇒ 底栏回来。只能在平板/真机上取。
3. 既有分栏帧取自 `34dae55`，**未在最终提交上重取**。后续两笔只改 `masterHidden` 分支，不影响那一档输出，但按证据纪律标注为「过程证据 / 未重取」，终验时统一重拍。

**关于那条未解现象（#191919 空色带）**：模拟器上**未复现** —— 左栏内容铺满到分隔条（正文可达 x=754、卡片容器到 786），账号所有者真机截图里那条约 360px 空色带对不上任何模拟器状态。**仍未归因**；本 ticket 未改动任何分栏几何，作为未解现象留在技术债登记里。

**一并记录的返工**：统筹者误传了账号所有者的裁定（把「全屏时隐藏底栏」说成「取整屏」），实现方据此提交了 `f4d3f30`，随后按更正提交 `5ba790a` 改为「隐藏」。历史里**保留这两笔、不 amend**，以留下更正痕迹。
